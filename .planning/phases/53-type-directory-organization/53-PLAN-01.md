---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/ipc.ts
  - src/types/index.ts
  - src/types/domain/api.ts
  - src/types/domain/ui.ts
  - src/types/domain/components.ts
  - src/types/domain/index.ts
  - env.d.ts
  - tsconfig.electron.json
  - src/clients/api.client.ts
  - src/clients/electron.client.ts
  - src/services/window.service.ts
  - src/services/favorites.service.ts
  - src/services/wallpaper.service.ts
  - src/services/collections.service.ts
  - src/services/download.service.ts
  - src/services/settings.service.ts
  - src/repositories/wallpaper.repository.ts
  - src/repositories/favorites.repository.ts
  - src/repositories/download.repository.ts
  - src/repositories/settings.repository.ts
  - src/repositories/window.repository.ts
  - src/composables/local/useLocalFiles.ts
  - src/composables/settings/useSettings.ts
  - electron/main/ipc/handlers/download.handler.ts
  - electron/main/ipc/handlers/cache.handler.ts
  - electron/preload/types.ts
autonomous: true
requirements: [TYPEORG-01, TYPEORG-02]
---

# Plan 01: Type Directory Organization

<objective>
整理类型定义目录结构，统一导入路径。将 `src/shared/types/ipc.ts` 移动到 `src/types/ipc.ts`，分类 `src/types/index.ts` 中的类型到 `domain/` 子目录，移除 `env.d.ts` 中的重复类型定义，并更新所有导入路径使用 `@/types/...` 别名。
</objective>

<tasks>

### Task 1: 创建 IPC 类型定义文件

**Type:** execute
**Files:**
- `src/types/ipc.ts` (新建)

<read_first>
- `src/shared/types/ipc.ts` (源文件，完整复制)

<action>
创建 `src/types/ipc.ts` 文件，内容为 `src/shared/types/ipc.ts` 的完整复制，但更新顶部注释：
- 将 `主进程导入方式：import { IPC_CHANNELS } from '../../src/shared/types/ipc'` 更新为 `主进程导入方式：import { IPC_CHANNELS } from '../../src/types/ipc'`
- 将 `渲染进程导入方式：import { IPC_CHANNELS } from '@/shared/types/ipc'` 更新为 `渲染进程导入方式：import { IPC_CHANNELS } from '@/types/ipc'`
</action>

<acceptance_criteria>
- [ ] 文件 `src/types/ipc.ts` 存在
- [ ] 文件包含 `export const IPC_CHANNELS`
- [ ] 文件包含 `export interface IpcResponse`
- [ ] 文件包含 `export interface DownloadProgressData`
- [ ] 文件包含 `export interface PendingDownload`
- [ ] 文件包含 `export interface ResumeDownloadParams`
- [ ] 文件顶部注释包含 `主进程导入方式：import { IPC_CHANNELS } from '../../src/types/ipc'`
- [ ] 文件顶部注释包含 `渲染进程导入方式：import { IPC_CHANNELS } from '@/types/ipc'`
</acceptance_criteria>

### Task 2: 创建 API 类型定义文件

**Type:** execute
**Files:**
- `src/types/domain/api.ts` (新建)

<read_first>
- `src/types/index.ts` (源类型定义)
- `src/types/domain/wallpaper.ts` (参考文件格式)

<action>
创建 `src/types/domain/api.ts` 文件，包含以下内容：

```typescript
/**
 * API 搜索参数类型定义
 * Phase 53: 从 src/types/index.ts 迁移
 */

// ==================== 搜索参数相关类型 ====================

/**
 * 自定义搜索参数
 */
export interface CustomParams {
  selector: number
  keyword: string
  categories: string[]
  aiArt: boolean
  purity: string[]
  sorting: string
  desc: boolean
  topRange: string
  ratios: string[]
  respickerLimitation: string
  resolutions: string[]
  resolution: string
  respickerCustomWidth: string
  respickerCustomHeight: string
  color: string
}

/**
 * API 获取参数
 */
export interface GetParams {
  q?: string
  ai_art_filter?: number
  categories?: string
  purity?: string
  sorting?: string
  topRange?: string
  order?: string
  colors?: string | null
  ratios?: string | null
  atleast?: string | null
  resolutions?: string | null
  page: number
  seed?: string | null
}
```
</action>

<acceptance_criteria>
- [ ] 文件 `src/types/domain/api.ts` 存在
- [ ] 文件包含 `export interface CustomParams`
- [ ] 文件包含 `export interface GetParams`
- [ ] CustomParams 接口包含 `selector: number` 属性
- [ ] GetParams 接口包含 `page: number` 属性
</acceptance_criteria>

### Task 3: 创建 UI 类型定义文件

**Type:** execute
**Files:**
- `src/types/domain/ui.ts` (新建)

<read_first>
- `src/types/index.ts` (源类型定义)
- `src/types/domain/wallpaper.ts` (参考文件格式)

<action>
创建 `src/types/domain/ui.ts` 文件，包含以下内容：

```typescript
/**
 * UI 辅助类型定义
 * Phase 53: 从 src/types/index.ts 迁移
 */

// ==================== UI 辅助类型 ====================

/**
 * 分辨率行数据
 */
export interface ResolutionLine {
  item: string[]
}

/**
 * 比例行数据
 */
export interface RatioLine {
  item: string[]
}

/**
 * 颜色行数据
 */
export interface ColorLine {
  item: string[]
}

/**
 * 壁纸操作信息（用于设置背景、下载等）
 */
export interface WallpaperActionInfo {
  id: string
  url: string
  size: number
  small: string
  resolution: string
}
```
</action>

<acceptance_criteria>
- [ ] 文件 `src/types/domain/ui.ts` 存在
- [ ] 文件包含 `export interface ResolutionLine`
- [ ] 文件包含 `export interface RatioLine`
- [ ] 文件包含 `export interface ColorLine`
- [ ] 文件包含 `export interface WallpaperActionInfo`
</acceptance_criteria>

### Task 4: 创建 Components 类型定义文件

**Type:** execute
**Files:**
- `src/types/domain/components.ts` (新建)

<read_first>
- `src/types/index.ts` (源类型定义)
- `src/types/domain/wallpaper.ts` (PageData 类型来源)

<action>
创建 `src/types/domain/components.ts` 文件，包含以下内容：

```typescript
/**
 * 组件 Props 类型定义
 * Phase 53: 从 src/types/index.ts 迁移
 */

import type { PageData } from './wallpaper'

// ==================== 组件 Props 类型 ====================

/**
 * SearchBar 组件 Props
 */
export interface SearchBarProps {
  customParams: import('./api').CustomParams
  apiKey: string
  desktopInfo: string
  saving: boolean
}

/**
 * WallpaperList 组件 Props
 */
export interface WallpaperListProps {
  pageData: PageData
  loading: boolean
  error: boolean
}
```
</action>

<acceptance_criteria>
- [ ] 文件 `src/types/domain/components.ts` 存在
- [ ] 文件包含 `import type { PageData } from './wallpaper'`
- [ ] 文件包含 `export interface SearchBarProps`
- [ ] 文件包含 `export interface WallpaperListProps`
- [ ] WallpaperListProps 接口的 pageData 类型为 `PageData`（非动态导入）
</acceptance_criteria>

### Task 5: 更新 domain/index.ts 重导出

**Type:** execute
**Files:**
- `src/types/domain/index.ts`

<read_first>
- `src/types/domain/index.ts` (当前内容)

<action>
更新 `src/types/domain/index.ts` 文件，在现有导出语句后添加新模块的导出：

```typescript
/**
 * 领域类型统一导出
 * Phase 46: 从 src/types/index.ts 迁移类型定义
 * Phase 53: 添加 api, ui, components 模块
 */

export * from './wallpaper'
export * from './favorite'
export * from './download'
export * from './settings'
export * from './api'
export * from './ui'
export * from './components'
```
</action>

<acceptance_criteria>
- [ ] 文件包含 `export * from './api'`
- [ ] 文件包含 `export * from './ui'`
- [ ] 文件包含 `export * from './components'`
</acceptance_criteria>

### Task 6: 重构 src/types/index.ts 为纯重导出入口

**Type:** execute
**Files:**
- `src/types/index.ts`

<read_first>
- `src/types/index.ts` (当前内容)

<action>
将 `src/types/index.ts` 重构为纯重导出入口：

```typescript
/**
 * 类型定义主入口
 * Phase 53: 重构为重导出入口
 *
 * 领域类型在 src/types/domain/ 目录
 * IPC 类型在 src/types/ipc.ts
 */

// 从 domain 目录重导出
export * from './domain'

// 从 ipc 文件重导出
export * from './ipc'
```
</action>

<acceptance_criteria>
- [ ] 文件包含 `export * from './domain'`
- [ ] 文件包含 `export * from './ipc'`
- [ ] 文件不包含 `export interface CustomParams` 定义
- [ ] 文件不包含 `export interface GetParams` 定义
- [ ] 文件不包含 `export interface ResolutionLine` 定义
- [ ] 文件不包含 `export interface WallpaperListProps` 定义
- [ ] 文件不包含 `export interface WallpaperActionInfo` 定义
</acceptance_criteria>

### Task 7: 重构 env.d.ts 移除重复类型定义

**Type:** execute
**Files:**
- `env.d.ts`

<read_first>
- `env.d.ts` (当前内容)
- `src/types/ipc.ts` (类型来源)

<action>
重构 `env.d.ts` 文件，移除重复的类型定义，从 `@/types/ipc` 导入：

```typescript
/// <reference types="vite/client" />

import type { Collection, FavoriteItem } from '@/types/domain/favorite'
import type { WallpaperItem } from '@/types/index'
import type {
  DownloadProgressData,
  IpcResponse,
  PendingDownload,
  ResumeDownloadParams,
} from '@/types/ipc'

// Electron API 类型声明
interface ElectronAPI {
  // 文件夹选择
  selectFolder: () => Promise<string | null>

  // 目录操作
  readDirectory: (dirPath: string) => Promise<{ error: string | null; files: any[] }>
  openFolder: (folderPath: string) => Promise<{ success: boolean; error?: string }>

  // 文件操作
  deleteFile: (filePath: string) => Promise<{ success: boolean; error: string | null }>
  checkFileExists: (
    filePath: string,
  ) => Promise<{ success: boolean; exists: boolean; error?: string }>

  // 下载功能
  downloadWallpaper: (params: {
    url: string
    filename: string
    saveDir: string
  }) => Promise<{ success: boolean; filePath: string | null; error: string | null }>

  // 带进度的下载任务
  startDownloadTask: (params: {
    taskId: string
    url: string
    filename: string
    saveDir: string
  }) => Promise<{
    success: boolean
    filePath: string | null
    error: string | null
    taskId?: string | null
  }>

  // 暂停下载任务
  pauseDownloadTask: (taskId: string) => Promise<{ success: boolean; error?: string }>

  // 取消下载任务
  cancelDownloadTask: (taskId: string) => Promise<{ success: boolean; error?: string }>

  // 恢复下载任务
  resumeDownloadTask: (params: ResumeDownloadParams) => Promise<IpcResponse<string>>

  // 获取待恢复的下载任务列表
  getPendingDownloads: () => Promise<IpcResponse<PendingDownload[]>>

  // 监听下载进度
  onDownloadProgress: (callback: (data: DownloadProgressData) => void) => void
  removeDownloadProgressListener: (callback: (data: DownloadProgressData) => void) => void

  // 壁纸设置
  setWallpaper: (imagePath: string) => Promise<{ success: boolean; error: string | null }>

  // Wallhaven API 代理
  wallhavenApiRequest: (params: { endpoint: string; params?: any; apiKey?: string }) => Promise<{
    success: boolean
    data: any | null
    error?: string
    status?: number
  }>

  // 窗口控制
  minimizeWindow: () => Promise<void>
  maximizeWindow: () => Promise<void>
  closeWindow: () => Promise<void>
  isMaximized: () => Promise<boolean>

  // Electron Store 操作
  storeGet: (key: string) => Promise<{ success: boolean; value: any; error?: string }>
  storeSet: (params: { key: string; value: any }) => Promise<{ success: boolean; error?: string }>
  storeDelete: (key: string) => Promise<{ success: boolean; error?: string }>
  storeClear: () => Promise<{ success: boolean; error?: string }>

  // 缓存管理
  clearAppCache: (downloadPath?: string) => Promise<{
    success: boolean
    thumbnailsDeleted: number
    tempFilesDeleted: number
    errors?: string[]
    error?: string
  }>
  getCacheInfo: (downloadPath?: string) => Promise<{
    success: boolean
    info: {
      thumbnailsCount: number
      tempFilesCount: number
    }
    error?: string
  }>

  // 孤儿文件清理
  cleanupOrphanFiles: (downloadPath: string) => Promise<{
    success: boolean
    filesDeleted: number
    stateFilesDeleted: number
    errors?: string[]
  }>

  // Favorites & Collections
  favoritesGetCollections: () => Promise<IpcResponse<Collection[]>>
  favoritesCreateCollection: (params: { name: string }) => Promise<IpcResponse<Collection>>
  favoritesRenameCollection: (params: { id: string; name: string }) => Promise<IpcResponse<Collection>>
  favoritesDeleteCollection: (params: { id: string }) => Promise<IpcResponse<void>>
  favoritesSetDefaultCollection: (params: { id: string }) => Promise<IpcResponse<Collection>>
  favoritesGetByCollection: (params: { collectionId?: string }) => Promise<IpcResponse<FavoriteItem[]>>
  favoritesAdd: (params: { wallpaperId: string; collectionId: string; wallpaperData: WallpaperItem }) => Promise<IpcResponse<FavoriteItem>>
  favoritesRemove: (params: { wallpaperId: string; collectionId: string }) => Promise<IpcResponse<void>>
  favoritesMove: (params: { wallpaperId: string; fromCollectionId: string; toCollectionId: string }) => Promise<IpcResponse<FavoriteItem>>
  favoritesIsFavorite: (params: { wallpaperId: string }) => Promise<IpcResponse<boolean>>
  favoritesGetCollectionsForWallpaper: (params: { wallpaperId: string }) => Promise<IpcResponse<Collection[]>>
  favoritesGetPaginated: (params: {
    collectionId?: string
    limit: number
    offset: number
  }) => Promise<IpcResponse<any>>
  favoritesGetCounts: () => Promise<IpcResponse<Record<string, number>>>
  favoritesGetStatusMap: (params: { wallpaperIds: string[] }) => Promise<IpcResponse<Record<string, 0 | 1 | 2>>>

  // 通用IPC通信
  send: (channel: string, data: any) => void
  receive: (channel: string, func: (...args: any[]) => void) => void
}

declare global {
  interface Window {
    electronAPI: ElectronAPI
  }
}

export {}
```
</action>

<acceptance_criteria>
- [ ] 文件包含 `import type { DownloadProgressData, IpcResponse, PendingDownload, ResumeDownloadParams } from '@/types/ipc'`
- [ ] 文件不包含本地 `interface DownloadProgressData` 定义
- [ ] 文件不包含本地 `interface ResumeDownloadParams` 定义
- [ ] 文件不包含本地 `interface PendingDownload` 定义
- [ ] 文件不包含本地 `interface IpcResponse` 定义
- [ ] 文件保留 `interface ElectronAPI` 定义
- [ ] 文件保留 `declare global { interface Window { electronAPI: ElectronAPI } }`
</acceptance_criteria>

### Task 8: 更新 tsconfig.electron.json

**Type:** execute
**Files:**
- `tsconfig.electron.json`

<read_first>
- `tsconfig.electron.json` (当前内容)

<action>
更新 `tsconfig.electron.json`，修改 `include` 路径：

```json
{
  "extends": "@tsconfig/node24/tsconfig.json",
  "include": ["electron/**/*", "src/types/ipc.ts"],
  "compilerOptions": {
    "composite": true,
    "types": ["electron-vite/node", "node"],
    "moduleResolution": "bundler",
    "outDir": "./out",
    "module": "ESNext"
  }
}
```
</action>

<acceptance_criteria>
- [ ] 文件 `include` 数组包含 `"src/types/ipc.ts"`
- [ ] 文件 `include` 数组不包含 `"src/shared/types/ipc.ts"`
</acceptance_criteria>

### Task 9: 更新渲染进程导入路径 (Clients)

**Type:** execute
**Files:**
- `src/clients/api.client.ts`
- `src/clients/electron.client.ts`

<read_first>
- `src/clients/api.client.ts`
- `src/clients/electron.client.ts`

<action>
将所有 `@/shared/types/ipc` 导入路径替换为 `@/types/ipc`：

1. `src/clients/api.client.ts`:
   - 将 `import type { IpcResponse } from '@/shared/types/ipc'` 更新为 `import type { IpcResponse } from '@/types/ipc'`

2. `src/clients/electron.client.ts`:
   - 将 `from '@/shared/types/ipc'` 更新为 `from '@/types/ipc'`
</action>

<acceptance_criteria>
- [ ] `src/clients/api.client.ts` 包含 `from '@/types/ipc'`
- [ ] `src/clients/api.client.ts` 不包含 `from '@/shared/types/ipc'`
- [ ] `src/clients/electron.client.ts` 包含 `from '@/types/ipc'`
- [ ] `src/clients/electron.client.ts` 不包含 `from '@/shared/types/ipc'`
</acceptance_criteria>

### Task 10: 更新渲染进程导入路径 (Services)

**Type:** execute
**Files:**
- `src/services/window.service.ts`
- `src/services/favorites.service.ts`
- `src/services/wallpaper.service.ts`
- `src/services/collections.service.ts`
- `src/services/download.service.ts`
- `src/services/settings.service.ts`

<read_first>
- `src/services/window.service.ts`
- `src/services/favorites.service.ts`
- `src/services/wallpaper.service.ts`
- `src/services/collections.service.ts`
- `src/services/download.service.ts`
- `src/services/settings.service.ts`

<action>
将所有 `@/shared/types/ipc` 导入路径替换为 `@/types/ipc`：

每个文件的修改：
- 将 `import type { ... } from '@/shared/types/ipc'` 更新为 `import type { ... } from '@/types/ipc'`
</action>

<acceptance_criteria>
- [ ] `src/services/window.service.ts` 包含 `from '@/types/ipc'`
- [ ] `src/services/favorites.service.ts` 包含 `from '@/types/ipc'`
- [ ] `src/services/wallpaper.service.ts` 包含 `from '@/types/ipc'`
- [ ] `src/services/collections.service.ts` 包含 `from '@/types/ipc'`
- [ ] `src/services/download.service.ts` 包含 `from '@/types/ipc'`
- [ ] `src/services/settings.service.ts` 包含 `from '@/types/ipc'`
- [ ] 所有 6 个文件都不包含 `from '@/shared/types/ipc'`
</acceptance_criteria>

### Task 11: 更新渲染进程导入路径 (Repositories)

**Type:** execute
**Files:**
- `src/repositories/wallpaper.repository.ts`
- `src/repositories/favorites.repository.ts`
- `src/repositories/download.repository.ts`
- `src/repositories/settings.repository.ts`
- `src/repositories/window.repository.ts`

<read_first>
- `src/repositories/wallpaper.repository.ts`
- `src/repositories/favorites.repository.ts`
- `src/repositories/download.repository.ts`
- `src/repositories/settings.repository.ts`
- `src/repositories/window.repository.ts`

<action>
将所有 `@/shared/types/ipc` 导入路径替换为 `@/types/ipc`：

每个文件的修改：
- 将 `import type { ... } from '@/shared/types/ipc'` 更新为 `import type { ... } from '@/types/ipc'`
</action>

<acceptance_criteria>
- [ ] `src/repositories/wallpaper.repository.ts` 包含 `from '@/types/ipc'`
- [ ] `src/repositories/favorites.repository.ts` 包含 `from '@/types/ipc'`
- [ ] `src/repositories/download.repository.ts` 包含 `from '@/types/ipc'`
- [ ] `src/repositories/settings.repository.ts` 包含 `from '@/types/ipc'`
- [ ] `src/repositories/window.repository.ts` 包含 `from '@/types/ipc'`
- [ ] 所有 5 个文件都不包含 `from '@/shared/types/ipc'`
</acceptance_criteria>

### Task 12: 更新渲染进程导入路径 (Composables)

**Type:** execute
**Files:**
- `src/composables/local/useLocalFiles.ts`
- `src/composables/settings/useSettings.ts`

<read_first>
- `src/composables/local/useLocalFiles.ts`
- `src/composables/settings/useSettings.ts`

<action>
将所有 `@/shared/types/ipc` 导入路径替换为 `@/types/ipc`：

每个文件的修改：
- 将 `import type { ... } from '@/shared/types/ipc'` 更新为 `import type { ... } from '@/types/ipc'`
</action>

<acceptance_criteria>
- [ ] `src/composables/local/useLocalFiles.ts` 包含 `from '@/types/ipc'`
- [ ] `src/composables/settings/useSettings.ts` 包含 `from '@/types/ipc'`
- [ ] 两个文件都不包含 `from '@/shared/types/ipc'`
</acceptance_criteria>

### Task 13: 更新主进程导入路径 (download.handler.ts)

**Type:** execute
**Files:**
- `electron/main/ipc/handlers/download.handler.ts`

<read_first>
- `electron/main/ipc/handlers/download.handler.ts`

<action>
更新 `electron/main/ipc/handlers/download.handler.ts` 的导入路径：
- 将 `from '../../../../src/shared/types/ipc'` 更新为 `from '../../../../src/types/ipc'`
</action>

<acceptance_criteria>
- [ ] 文件包含 `from '../../../../src/types/ipc'`
- [ ] 文件不包含 `from '../../../../src/shared/types/ipc'`
</acceptance_criteria>

### Task 14: 更新主进程导入路径 (cache.handler.ts)

**Type:** execute
**Files:**
- `electron/main/ipc/handlers/cache.handler.ts`

<read_first>
- `electron/main/ipc/handlers/cache.handler.ts`

<action>
更新 `electron/main/ipc/handlers/cache.handler.ts` 的导入路径：
- 将 `from '../../../../src/shared/types/ipc'` 更新为 `from '../../../../src/types/ipc'`
</action>

<acceptance_criteria>
- [ ] 文件包含 `from '../../../../src/types/ipc'`
- [ ] 文件不包含 `from '../../../../src/shared/types/ipc'`
</acceptance_criteria>

### Task 15: 更新主进程导入路径 (preload/types.ts)

**Type:** execute
**Files:**
- `electron/preload/types.ts`

<read_first>
- `electron/preload/types.ts`

<action>
更新 `electron/preload/types.ts` 的所有导入路径：
- 将所有 `from '../../src/shared/types/ipc'` 更新为 `from '../../src/types/ipc'`
</action>

<acceptance_criteria>
- [ ] 文件包含 `from '../../src/types/ipc'`
- [ ] 文件不包含 `from '../../src/shared/types/ipc'`
</acceptance_criteria>

### Task 16: 删除旧的 shared/types 目录

**Type:** execute
**Files:**
- `src/shared/types/ipc.ts` (删除)
- `src/shared/types/` 目录 (删除，如果为空)

<read_first>
- `src/shared/types/ipc.ts` (确认存在)

<action>
1. 删除 `src/shared/types/ipc.ts` 文件
2. 删除 `src/shared/types/` 目录（如果为空）
3. 删除 `src/shared/` 目录（如果为空）
</action>

<acceptance_criteria>
- [ ] 文件 `src/shared/types/ipc.ts` 不存在
- [ ] 目录 `src/shared/types/` 不存在
- [ ] 目录 `src/shared/` 不存在（如果之前只有 types 子目录）
</acceptance_criteria>

</tasks>

<verification>
1. 运行 `pnpm typecheck` 确保无 TypeScript 错误
2. 运行 `pnpm build` 确保构建成功
3. 验证无遗留的 `@/shared/types/ipc` 导入路径：
   - `grep -r "@/shared/types/ipc" src/` 应无结果
4. 验证无遗留的 `shared/types/ipc` 导入路径：
   - `grep -r "shared/types/ipc" electron/` 应无结果
5. 验证目录结构：
   - `ls src/types/` 应显示 `index.ts`, `ipc.ts`, `domain/`
   - `ls src/types/domain/` 应显示 `api.ts`, `ui.ts`, `components.ts` 等文件
</verification>

<success_criteria>
- TypeScript 编译通过 (`pnpm typecheck` 退出码 0)
- 构建成功 (`pnpm build` 退出码 0)
- 所有类型导入使用 `@/types/...` 路径别名
- 类型定义目录结构清晰：`domain/`, `ipc.ts`
- `src/shared/types/` 目录已删除
- `env.d.ts` 无重复类型定义
</success_criteria>

<must_haves>
<truths>
- 所有类型定义必须保持语义不变
- 不修改任何功能行为或业务逻辑
- 主进程文件使用相对路径导入
- 渲染进程文件使用 `@/types/...` 别名
- `env.d.ts` 必须保留 `ElectronAPI` 接口和 `Window` 扩展
</truths>
</must_haves>
