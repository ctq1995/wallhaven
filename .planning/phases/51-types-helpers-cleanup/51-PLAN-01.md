---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - env.d.ts
  - src/types/favorite.ts
  - src/types/api/index.ts
  - src/types/ipc/index.ts
autonomous: true
requirements:
  - DEADTYPE-01
  - DEADTYPE-02
  - DEADTYPE-03
---

<objective>

清理项目中的死代码，包括重复类型定义、空导出文件。具体包括：
1. 更新 `env.d.ts` 的导入路径
2. 删除重复类型文件 `src/types/favorite.ts`
3. 删除空导出文件 `src/types/api/index.ts` 和目录
4. 删除空导出文件 `src/types/ipc/index.ts` 和目录

</objective>

<tasks>

## Task 1: 更新 env.d.ts 导入路径

<type>execute</type>
<files>
  - env.d.ts
</files>

<read_first>
  - env.d.ts
  - src/types/domain/favorite.ts
</read_first>

<action>

将 `env.d.ts` 文件中第 3 行的导入路径从 `@/types/favorite` 修改为 `@/types/domain/favorite`：

**修改前：**
```typescript
import type { Collection, FavoriteItem } from '@/types/favorite'
```

**修改后：**
```typescript
import type { Collection, FavoriteItem } from '@/types/domain/favorite'
```

</action>

<verify>
运行 `npm run type-check` 验证 TypeScript 编译无错误。
</verify>

<acceptance_criteria>
- `env.d.ts` 文件第 3 行包含字符串 `import type { Collection, FavoriteItem } from '@/types/domain/favorite'`
- `npm run type-check` 命令执行后输出无错误
</acceptance_criteria>

---

## Task 2: 删除重复类型文件 src/types/favorite.ts

<type>execute</type>
<files>
  - src/types/favorite.ts
</files>

<read_first>
  - src/types/favorite.ts
  - env.d.ts
</read_first>

<action>

删除文件 `src/types/favorite.ts`。

执行命令：
```bash
rm src/types/favorite.ts
```

</action>

<verify>
运行 `npm run type-check` 验证 TypeScript 编译无错误。
</verify>

<acceptance_criteria>
- `src/types/favorite.ts` 文件不存在（`test -f src/types/favorite.ts` 返回非零）
- `npm run type-check` 命令执行后输出无错误
</acceptance_criteria>

---

## Task 3: 删除空导出目录 src/types/api/

<type>execute</type>
<files>
  - src/types/api/index.ts
</files>

<read_first>
  - src/types/api/index.ts
</read_first>

<action>

删除文件 `src/types/api/index.ts` 和目录 `src/types/api/`。

执行命令：
```bash
rm src/types/api/index.ts
rmdir src/types/api
```

</action>

<verify>
运行 `npm run type-check` 验证 TypeScript 编译无错误。
</verify>

<acceptance_criteria>
- `src/types/api/index.ts` 文件不存在
- `src/types/api/` 目录不存在
- `npm run type-check` 命令执行后输出无错误
</acceptance_criteria>

---

## Task 4: 删除空导出目录 src/types/ipc/

<type>execute</type>
<files>
  - src/types/ipc/index.ts
</files>

<read_first>
  - src/types/ipc/index.ts
</read_first>

<action>

删除文件 `src/types/ipc/index.ts` 和目录 `src/types/ipc/`。

执行命令：
```bash
rm src/types/ipc/index.ts
rmdir src/types/ipc
```

</action>

<verify>
运行 `npm run type-check` 验证 TypeScript 编译无错误。
</verify>

<acceptance_criteria>
- `src/types/ipc/index.ts` 文件不存在
- `src/types/ipc/` 目录不存在
- `npm run type-check` 命令执行后输出无错误
</acceptance_criteria>

---

## Task 5: 完整验证

<type>execute</type>
<files>
</files>

<read_first>
</read_first>

<action>

运行完整的类型检查和 lint 检查，确保所有更改无破坏性影响。

执行命令：
```bash
npm run type-check
npm run lint
```

</action>

<verify>
两个命令都执行成功，无错误输出。
</verify>

<acceptance_criteria>
- `npm run type-check` 命令执行后输出无错误
- `npm run lint` 命令执行后输出无错误
- `src/types/favorite.ts` 文件不存在
- `src/types/api/` 目录不存在
- `src/types/ipc/` 目录不存在
- `env.d.ts` 包含字符串 `@/types/domain/favorite`
</acceptance_criteria>

</tasks>

<verification>

### 验证清单

1. **文件删除验证**
   - `test ! -f src/types/favorite.ts` 返回 true
   - `test ! -d src/types/api` 返回 true
   - `test ! -d src/types/ipc` 返回 true

2. **导入路径验证**
   - `grep -q "@/types/domain/favorite" env.d.ts` 返回 true

3. **TypeScript 编译验证**
   - `npm run type-check` 执行成功，无错误输出

4. **ESLint 验证**
   - `npm run lint` 执行成功，无错误输出

</verification>

<success_criteria>

1. TypeScript 编译通过：`npm run type-check` 无错误
2. ESLint 检查通过：`npm run lint` 无错误
3. 应用正常启动并运行
4. 所有现有功能保持不变

</success_criteria>

<must_haves>

- `env.d.ts` 导入路径必须从 `@/types/favorite` 改为 `@/types/domain/favorite`
- `src/types/favorite.ts` 文件必须被删除
- `src/types/api/` 目录必须被删除
- `src/types/ipc/` 目录必须被删除
- `npm run type-check` 必须无错误通过
- `npm run lint` 必须无错误通过

</must_haves>
