---
phase: 41-database-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/main/sqlite.d.ts
  - package.json
autonomous: true
requirements:
  - DBINFRA-02
user_setup: []

must_haves:
  truths:
    - "TypeScript compilation succeeds when importing from 'node:sqlite' module in main process code"
    - "package.json engines field requires Node.js >= 24 so node:sqlite is available in all environments"
  artifacts:
    - path: "electron/main/sqlite.d.ts"
      provides: "TypeScript type declarations for node:sqlite module (DatabaseSync, StatementSync classes)"
      contains: "declare module 'node:sqlite'"
    - path: "package.json"
      provides: "Node.js version requirement enforcement"
      contains: "\"node\": \">=24\""
  key_links:
    - from: "electron/main/sqlite.d.ts"
      to: "electron/main/database.ts"
      via: "import { DatabaseSync } from 'node:sqlite'"
      pattern: "from 'node:sqlite'"
---

<objective>
Create the foundational type declarations for node:sqlite and update the project Node.js version requirement.

Purpose: TypeScript cannot resolve node:sqlite module types from @types/node (Stability 1.1, not included in DefinitelyTyped). A custom declaration file is required. The engines field must enforce Node.js >= 24 to guarantee node:sqlite availability.

Output:
- electron/main/sqlite.d.ts — custom type declarations for node:sqlite (DatabaseSync, StatementSync)
- package.json — updated engines.node to ">=24"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@package.json

The exact type declaration content is specified in STACK.md under "TypeScript Type Declaration" section. Use that as the authoritative source. The declaration covers the full API surface that Phase 41 uses: DatabaseSync (constructor, close, exec, open, prepare), StatementSync (all, get, run, iterate, columns), RunResult, DatabaseOptions, ColumnInfo, BindParams.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create node:sqlite TypeScript type declarations</name>
  <files>electron/main/sqlite.d.ts</files>
  <read_first>
    - .planning/research/STACK.md (full TypeScript type declaration section, lines 338-389)
    - .planning/codebase/CONVENTIONS.md (type declaration conventions — use interface over type for object shapes)
  </read_first>
  <action>
    Create electron/main/sqlite.d.ts with the following exact content structure:

    1. A `declare module 'node:sqlite'` module augmentation block.

    2. Inside the module declaration, define:
       - A type alias `BindParams = Record<string, unknown> | unknown[]`
       - An interface `RunResult` with fields: `lastInsertRowid: number`, `changes: number`
       - An interface `DatabaseOptions` with optional fields: `open?: boolean`, `readOnly?: boolean`, `enableForeignKeyConstraints?: boolean`, `timeout?: number`
       - An interface `ColumnInfo` with fields: `name: string`, `column: string | null`, `table: string | null`, `database: string | null`, `type: string | null`
       - An exported class `StatementSync<T extends Record<string, unknown> = Record<string, unknown>>` with methods: `all(...params: BindParams[]): T[]`, `get(...params: BindParams[]): T | undefined`, `run(...params: BindParams[]): RunResult`, `iterate(...params: BindParams[]): IterableIterator<T>`, `columns(): ColumnInfo[]`, and readonly properties: `sourceSQL: string`, `expandedSQL: string`
       - An exported class `DatabaseSync` with constructor signature `constructor(location: string, options?: DatabaseOptions)`, methods: `close(): void`, `exec(sql: string): void`, `open(): void`, `prepare<T extends Record<string, unknown> = Record<string, unknown>>(sql: string): StatementSync<T>`, and readonly properties: `isOpen: boolean`, `isTransaction: boolean`

    3. Follow project conventions from CONVENTIONS.md: use `interface` for object shapes (RunResult, DatabaseOptions, ColumnInfo), use `type` for the BindParams union type.

    4. Add a file header comment explaining purpose: "Custom type declarations for node:sqlite (Stability 1.1 — @types/node does not include these). Minimal surface covering only what this project uses."

    Source reference: .planning/research/STACK.md lines 338-389 (the exact declaration content is specified there). Adapt to match CONVENTIONS.md preference for interfaces over types where appropriate.
  </action>
  <verify>
    <automated>grep -c "declare module 'node:sqlite'" electron/main/sqlite.d.ts && grep -c "export class DatabaseSync" electron/main/sqlite.d.ts && grep -c "export class StatementSync" electron/main/sqlite.d.ts</automated>
  </verify>
  <acceptance_criteria>
    1. File electron/main/sqlite.d.ts exists and is non-empty
    2. Contains `declare module 'node:sqlite'` at line 1 or 2 (after comment header)
    3. Contains `export class DatabaseSync` — at minimum 20 lines of class definition with constructor, close, exec, open, prepare
    4. Contains `export class StatementSync` — at minimum 20 lines with all, get, run, iterate, columns
    5. Contains `BindParams` type and `RunResult`, `DatabaseOptions`, `ColumnInfo` interfaces
    6. Total file is at least 25 lines
    7. Uses `interface` for object shapes (not `type`)
  </acceptance_criteria>
  <done>electron/main/sqlite.d.ts exists with DatabaseSync, StatementSync, RunResult, DatabaseOptions, ColumnInfo, BindParams types. Total >= 25 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json engines field to require Node.js >= 24</name>
  <files>package.json</files>
  <read_first>
    - package.json (current engines field at line 68-70)
  </read_first>
  <action>
    Modify the `engines.node` field in package.json:

    CURRENT: `"node": "^20.19.0 || >=22.12.0"`
    NEW: `"node": ">=24"`

    Rationale per D-08: node:sqlite ships with Node.js 24+ at Stability 1.1 with no experimental flag required. The project's development environment, CI runners, and Electron 41's bundled Node.js (v24.14.0+) all satisfy this constraint.

    Change only the `engines.node` value. Do NOT modify any other part of package.json.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); process.exit(p.engines.node === '>=24' ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    1. package.json engines.node exactly equals ">=24"
    2. No other fields in package.json are modified
    3. `node -e "const p = require('./package.json'); console.log(p.engines.node)"` outputs ">=24"
  </acceptance_criteria>
  <done>package.json engines.node updated from "^20.19.0 || >=22.12.0" to ">=24".</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plan 41-01 creates type declarations and config only — no runtime security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | {E} | package.json engines | accept | engines field is advisory (npm install warns but does not block). The project's CI and development environments must ensure Node.js >= 24. Electron 41 bundles Node.js 24.14.0 which satisfies this. |
</threat_model>

<verification>
1. Run `node -e "const p = require('./package.json'); console.log(p.engines.node)"` — must output ">=24"
2. Run `grep -c "declare module 'node:sqlite'" electron/main/sqlite.d.ts` — must output >= 1
3. Run `grep -c "export class DatabaseSync" electron/main/sqlite.d.ts` — must output >= 1
4. Verify total lines in sqlite.d.ts >= 25: `wc -l electron/main/sqlite.d.ts`
</verification>

<success_criteria>
1. sqlite.d.ts file exists with all required types
2. package.json engines.node updated to ">=24"
3. TypeScript compilation can resolve 'node:sqlite' module imports in main process code
</success_criteria>

<output>
After completion, create `.planning/phases/41-database-infrastructure/41-01-SUMMARY.md`
</output>
