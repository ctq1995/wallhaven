---
wave: 6
depends_on: [45-05]
files_modified: []
files_deleted: []
autonomous: false
requirements: [VER-01, VER-03, VER-05]
---

# Plan 45-06: Final Build and Functional Verification

## Objective

最终验证构建成功和功能完整性。此计划需要手动功能测试。

## Context

完成所有清理工作后，需要验证：
1. 应用可正常构建（VER-05）
2. 应用可正常启动和运行（VER-03）
3. 所有核心功能正常工作（VER-01）

## Tasks

### Task 1: Final Build Verification

<read_first>
- package.json
</read_first>

<action>
运行完整构建命令验证最终代码可正常编译和打包
</action>

<acceptance_criteria>
- `npm run build` 命令成功完成，退出码为 0
- 输出目录 `out/` 包含完整的应用文件
- `out/main/index.js` 存在
- `out/preload/index.mjs` 存在
- `out/renderer/index.html` 存在
- 无 TypeScript 编译错误
- 无 Vite 打包错误
</acceptance_criteria>

---

### Task 2: Verify No electron-store References

<read_first>
- .planning/phases/45-cleanup-final-verification/45-RESEARCH.md
</read_first>

<action>
全局搜索确认无残留的 electron-store 引用
</action>

<acceptance_criteria>
- `grep -r "electron-store" --include="*.ts" --include="*.js" --include="*.json" .` 仅显示 node_modules 或无关文件
- `grep -r "from 'electron-store'" --include="*.ts" .` 返回空
- `grep -r "from \"electron-store\"" --include="*.ts" .` 返回空
- `grep -rn "save-settings" --include="*.ts" .` 返回空（除可能的注释/文档）
- `grep -rn "load-settings" --include="*.ts" .` 返回空（除可能的注释/文档）
</acceptance_criteria>

---

### Task 3: Manual Functional Verification (VER-01)

<read_first>
- .planning/REQUIREMENTS.md
</read_first>

<action>
启动应用进行手动功能测试：

1. **启动测试**：
   ```bash
   npm run dev
   ```
   - 观察控制台日志确认数据库初始化成功
   - 确认无 `[Migration] Migration failed` 错误

2. **设置功能测试**：
   - 打开设置页面
   - 修改下载路径或其他设置
   - 保存设置
   - 重启应用，确认设置已保存

3. **搜索功能测试**：
   - 在搜索框输入关键词搜索
   - 确认搜索结果显示
   - 翻页测试

4. **下载功能测试**：
   - 选择壁纸启动下载
   - 观察下载进度
   - 确认下载完成

5. **收藏功能测试**：
   - 添加壁纸到收藏
   - 切换收藏夹
   - 移除收藏
   - 确认小红心状态正确
</action>

<acceptance_criteria>
- 应用成功启动，无崩溃
- 控制台无错误日志
- 设置保存和读取正常
- 搜索功能正常
- 下载功能正常
- 收藏功能正常（包括 wallpaperData 序列化正确）
</acceptance_criteria>

---

### Task 4: Startup Performance Verification (VER-03)

<read_first>
- electron/main/database.ts
</read_first>

<action>
验证应用启动性能：
1. 观察控制台中 `[Migration]` 日志的时间戳
2. 确认数据库初始化时间 < 500ms

对于已迁移用户：
- 应看到 `[Migration] Already migrated (found _migrated_from_store)`

对于新用户：
- 应看到 `[Migration] No electron-store file found (fresh install)`
</action>

<acceptance_criteria>
- 应用启动时间正常（< 5 秒到窗口显示）
- 数据库初始化无阻塞
- 控制台无性能警告
</acceptance_criteria>

---

## Verification

1. 构建成功：`npm run build` 退出码 0
2. 无残留引用：grep 搜索确认
3. 功能测试：手动测试确认所有功能正常
4. 性能验证：启动时间正常

## must_haves

- [ ] npm run build 成功
- [ ] 无 electron-store 残留引用
- [ ] 应用启动正常
- [ ] 设置功能正常
- [ ] 搜索功能正常
- [ ] 下载功能正常
- [ ] 收藏功能正常
- [ ] 启动性能正常（< 500ms 数据库初始化）
