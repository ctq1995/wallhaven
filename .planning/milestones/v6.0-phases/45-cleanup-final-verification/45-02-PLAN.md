---
wave: 2
depends_on: [45-01]
files_modified:
  - electron/main/ipc/handlers/index.ts
files_deleted:
  - electron/main/ipc/handlers/settings.handler.ts
autonomous: true
requirements: [CLN-03, CLN-06]
---

# Plan 45-02: Delete settings.handler.ts and Its Registration

## Objective

删除 `settings.handler.ts` 文件及其所有注册，移除 `save-settings` 和 `load-settings` IPC 通道。

## Context

根据 RESEARCH.md 分析：
- `settings.handler.ts` 的 `save-settings`/`load-settings` 通道是死代码
- `SettingPage.vue` 已通过 `useSettings` composable 使用 `store-set`/`store-get` 通道
- 该文件没有任何活跃的消费者

## Tasks

### Task 1: Remove settings.handler.ts Import and Registration

<read_first>
- electron/main/ipc/handlers/index.ts
</read_first>

<action>
修改 `electron/main/ipc/handlers/index.ts`：

1. 删除第 11 行的导入语句：
```typescript
import { registerSettingsHandlers } from './settings.handler'
```

2. 删除第 77 行的注册调用：
```typescript
registerSettingsHandlers()
```

3. 从 `REGISTERED_CHANNELS` 数组（第 39-40 行）删除：
```typescript
'save-settings',
'load-settings',
```
</action>

<acceptance_criteria>
- `grep -n "registerSettingsHandlers" electron/main/ipc/handlers/index.ts` 返回空
- `grep -n "settings.handler" electron/main/ipc/handlers/index.ts` 返回空
- `grep -n "save-settings" electron/main/ipc/handlers/index.ts` 返回空
- `grep -n "load-settings" electron/main/ipc/handlers/index.ts` 返回空
- `REGISTERED_CHANNELS` 数组长度减少 2（从 28 变为 26）
</acceptance_criteria>

---

### Task 2: Delete settings.handler.ts File

<read_first>
- electron/main/ipc/handlers/settings.handler.ts
</read_first>

<action>
删除文件 `electron/main/ipc/handlers/settings.handler.ts`
</action>

<acceptance_criteria>
- 文件 `electron/main/ipc/handlers/settings.handler.ts` 不存在
- `ls electron/main/ipc/handlers/settings.handler.ts` 返回错误
</acceptance_criteria>

---

### Task 3: Verify Build After Handler Removal

<read_first>
- package.json
</read_first>

<action>
运行构建命令验证删除后代码可正常编译
</action>

<acceptance_criteria>
- `npm run build` 命令成功完成，退出码为 0
- 无 TypeScript 编译错误
</acceptance_criteria>

---

## Verification

1. grep 确认 index.ts 中无 settings.handler 引用
2. 文件系统确认 settings.handler.ts 已删除
3. 构建成功确认无编译错误

## must_haves

- [ ] settings.handler.ts 文件已删除
- [ ] index.ts 中无 registerSettingsHandlers 导入和调用
- [ ] REGISTERED_CHANNELS 中无 save-settings/load-settings
- [ ] 构建成功
