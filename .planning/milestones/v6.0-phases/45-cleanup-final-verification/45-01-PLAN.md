---
wave: 1
depends_on: []
files_modified: []
files_deleted: []
autonomous: true
requirements: [VER-01, VER-03]
---

# Plan 45-01: Verify CR-01/CR-02 Fix Status

## Objective

验证 Phase 44 REVIEW 中的两个 CRITICAL issues 是否已正确修复。根据 RESEARCH.md 分析，这两个问题已在代码中修复，本计划仅做验证确认。

## Context

- **CR-01**: Database connection reset on migration failure — `database.ts` 应有 try-catch 处理
- **CR-02**: wallpaperData serialization inconsistency — `migration.ts` 应使用统一的 `JSON.stringify()`

## Tasks

### Task 1: Verify CR-01 Fix (Database Connection Reset)

<read_first>
- electron/main/database.ts
</read_first>

<action>
验证 `database.ts:185-196` 的迁移错误处理逻辑：
1. 确认 try-catch 块存在
2. 确认 `db.close()` 和 `db = undefined` 在 catch 块中执行
</action>

<acceptance_criteria>
- `grep -A 10 "runMigration(db)" electron/main/database.ts` 显示 try-catch 块
- `grep -A 3 "catch (error)" electron/main/database.ts` 显示 `db.close()` 和 `db = undefined`
</acceptance_criteria>

---

### Task 2: Verify CR-02 Fix (wallpaperData Serialization)

<read_first>
- electron/main/migration.ts
</read_first>

<action>
验证 `migration.ts:158` 使用 `JSON.stringify(f.wallpaperData)` 统一序列化
</action>

<acceptance_criteria>
- `grep "wallpaperData" electron/main/migration.ts` 显示 `JSON.stringify(f.wallpaperData)`
- 不存在条件序列化逻辑（如 `typeof f.wallpaperData === 'string' ? f.wallpaperData : ...`）
</acceptance_criteria>

---

### Task 3: Verify Build Succeeds

<read_first>
- package.json
</read_first>

<action>
运行构建命令验证当前代码可正常编译
</action>

<acceptance_criteria>
- `npm run build` 命令成功完成，退出码为 0
- 输出目录 `out/` 包含 main、preload、renderer 子目录
</acceptance_criteria>

---

## Verification

1. CR-01 验证：grep 搜索确认 database.ts 中迁移错误处理正确
2. CR-02 验证：grep 搜索确认 migration.ts 中 wallpaperData 序列化正确
3. 构建验证：`npm run build` 成功

## must_haves

- [ ] CR-01 修复确认：database.ts 有完整的迁移错误处理
- [ ] CR-02 修复确认：migration.ts 使用统一的 JSON.stringify
- [ ] 构建成功：npm run build 无错误
