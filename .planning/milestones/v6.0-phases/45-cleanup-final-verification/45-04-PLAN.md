---
wave: 4
depends_on: [45-03]
files_modified:
  - src/shared/types/ipc.ts
  - env.d.ts
autonomous: true
requirements: [CLN-06]
---

# Plan 45-04: Clean IPC Enums and Type Definitions

## Objective

从 IPC 通道枚举和类型声明文件中移除 `SAVE_SETTINGS`/`LOAD_SETTINGS` 相关定义。

## Context

- `src/shared/types/ipc.ts` 包含 `SAVE_SETTINGS` 和 `LOAD_SETTINGS` 枚举值
- `env.d.ts` 包含 ElectronAPI 接口中的 `saveSettings` 和 `loadSettings` 方法声明
- 这些定义已无消费者，可安全删除

## Tasks

### Task 1: Remove IPC Channel Enums

<read_first>
- src/shared/types/ipc.ts
</read_first>

<action>
修改 `src/shared/types/ipc.ts`：

从 `IPC_CHANNELS` 对象（第 36-37 行）删除：
```typescript
SAVE_SETTINGS: 'save-settings',
LOAD_SETTINGS: 'load-settings',
```
</action>

<acceptance_criteria>
- `grep -n "SAVE_SETTINGS" src/shared/types/ipc.ts` 返回空
- `grep -n "LOAD_SETTINGS" src/shared/types/ipc.ts` 返回空
- `grep -n "save-settings" src/shared/types/ipc.ts` 返回空
- `grep -n "load-settings" src/shared/types/ipc.ts` 返回空
</acceptance_criteria>

---

### Task 2: Remove Related Type Definitions (Optional)

<read_first>
- src/shared/types/ipc.ts
</read_first>

<action>
检查并删除 `src/shared/types/ipc.ts` 中不再使用的类型定义：

1. `SaveSettingsRequest` 接口（如存在）
2. `SaveSettingsResponse` 接口（如存在）
3. `LoadSettingsResponse` 接口（如存在）

注意：根据文件内容，这些类型可能在第 267-288 行附近
</action>

<acceptance_criteria>
- `grep -n "SaveSettings" src/shared/types/ipc.ts` 返回空
- `grep -n "LoadSettings" src/shared/types/ipc.ts` 返回空
</acceptance_criteria>

---

### Task 3: Update env.d.ts ElectronAPI Interface

<read_first>
- env.d.ts
</read_first>

<action>
修改 `env.d.ts`：

从 ElectronAPI 接口（第 108-109 行）删除：
```typescript
saveSettings: (settings: any) => Promise<{ success: boolean; error?: string }>
loadSettings: () => Promise<{ success: boolean; settings: any | null; error?: string }>
```
</action>

<acceptance_criteria>
- `grep -n "saveSettings" env.d.ts` 返回空
- `grep -n "loadSettings" env.d.ts` 返回空
</acceptance_criteria>

---

### Task 4: Verify Build After Type Cleanup

<read_first>
- package.json
</read_first>

<action>
运行构建命令验证修改后代码可正常编译
</action>

<acceptance_criteria>
- `npm run build` 命令成功完成，退出码为 0
- 无 TypeScript 编译错误
</acceptance_criteria>

---

## Verification

1. grep 确认 ipc.ts 中无 SAVE_SETTINGS/LOAD_SETTINGS
2. grep 确认 env.d.ts 中无 saveSettings/loadSettings
3. 构建成功确认无编译错误

## must_haves

- [ ] ipc.ts 中无 SAVE_SETTINGS/LOAD_SETTINGS 枚举
- [ ] ipc.ts 中无相关类型定义
- [ ] env.d.ts 中无 saveSettings/loadSettings 方法声明
- [ ] 构建成功
